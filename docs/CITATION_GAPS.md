# Citation gaps

Records whose sources are missing or weaker than the standard the atlas sets for itself. The build test fails if more than 3 percent of fact-bearing records have empty `sources`.

No records currently have empty sources. Entries below note places where the citation is a standard secondary reference rather than primary literature, or where the record is still forming (marked `verification: developing` in the data).
